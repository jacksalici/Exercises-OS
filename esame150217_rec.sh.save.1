
#!/bin/sh
cd $1
flag=1
stampager=1

for i in *
	do
		if test -f $i -a -x $i
		then 
			if test `wc -l < $i` = $2
			then
				for j in `cat $i`
				do
					if grep [!0-9] $i
					then 
						flag=0
						break;

					fi

				done

				if test $flag = 1
				then 
					if $stampager = 1
					then
						stampager=0
						echo cartella `pwd` >> /tmp/f150217.txt
					fi
					echo file $i >> /tmp/f150217.txt				fi
			fi
		fi
	done

for i in *
do
	if test -x $i -a -d $i
	then
		.././esame150217_rec.sh `pwd`/$i $2
http://www.didattica.agentgroup.unimo.it/didattica/SOeLab/SessioniInterattive/Laboratorio/EsercitazioneVenerd%C3%AC9Aprile2021.pdf	fi
done 

pwd
